# macOS
.DS_Store
**/.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Node / build artifacts (if ever added)
node_modules/
dist/
build/

# Workspace / tooling caches
archive/
.cache/

# Local env
.env
.env.local

# Internal — not for the live deploy (backups, snapshots, agent + checklist files)
.claude/
**/.claude/
versions/
tms/old/
GO-LIVE.md
**/GO-LIVE.md
*.bak
*.bak-*

# Aegis is pre-launch (homepage points to coming-soon) — not for live deploy yet
aegis/
.indexnow-key

# offer-lock — this repo is public and deploys to dion-group.com, so the
# Offer Builder's plaintext sources, the locked builds and above all the
# passphrase must never be committed. Only the tooling is versioned.
tools/offer-lock/src/
tools/offer-lock/dist/
tools/offer-lock/node_modules
passphrase.txt
*.passphrase
.passphrase*
